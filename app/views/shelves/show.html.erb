<h1><%= @shelf.name %> Page</h1>

<div>
  <% if @shelf.movies %>
    <h3>Movies in this shelf:</h3>
    <ul>
      <% @shelf.movies.each do |movie| %>
        <li>
          <%= link_to movie.title, movie_path(movie) %>
        </li>
      <% end %>
    </ul>
  <% end %>
</div>

